spring:
  devtools:
     livereload: 
       enabled: true 
  datasource:
    url: jdbc:h2:c:/tmp/db/wexpenses8-devyml;AUTO_SERVER=TRUE
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: create-drop
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
    show-sql: true
    properties:
      hibernate:
        show_sql: true
        format_sql: false

logging:
  file:
    name: c:/tmp/logs/wexpenses8.log
  pattern:
    file: "%d [%t] %5p %c{1}:%L - %m%n"
    console: "%d [%t] %-5level %logger : %msg%n"
  level:
    org.springframework.web: DEBUG        

server:  
  port: 8090    
  max-http-header-size: 10000000
  servlet:
    context-path: /wexpenses8

joinfaces:
  jsf:
    PROJECT_STAGE: Development

wexpenses:
  value:
    currency: CHF
    precision: 0.05
  currencies:
    CH: CHF
    DE: EUR
    ES: EUR
    FR: EUR
    NL: EUR
    IT: EUR
    EU: EUR
    JP: JPY
    US: USD
    UK: GBP

---
spring:
  profiles: work
  datasource:
    url: jdbc:h2:C:/Users/willi/git/wexpenses8/db/wexpenses-work-v2;AUTO_SERVER=TRUE
  jpa:
    hibernate:
      ddl-auto: none

